# from pathlib import Path
# str(Path().resolve())

BOOTSTRAP_SERVER = 'localhost:9092'
DSN='DRIVER=ODBC Driver 13 for SQL Server;SERVER=<server>;DATABASE=<database>;UID=<username>;PWD=<password>'
GROUP_ID = 'test-group'
TOPICS = ['foo-topic','bar-topic','baz-topic']